export type QRData = {
    prod_id: number,
    production_date: string,
    expiry_date: string,
    type_name: string,
    amount: number,
    units: string,
    type_id: number,
    nutritional: number,
    measure_type: string,
    allergens: string | null,
    cat_name: string,
    cat_id: number
}